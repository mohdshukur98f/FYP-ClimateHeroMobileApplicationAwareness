class User {
  String name, email, phone, password, point, dateregister, totalComplete;

  User(
      {this.name,
      this.email,
      this.phone,
      this.password,
      this.point,
      this.dateregister,
      this.totalComplete});
}
